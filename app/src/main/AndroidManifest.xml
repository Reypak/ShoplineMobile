<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matt.shopline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.PostView" />
        <activity android:name=".screens.Orders" />
        <activity
            android:name=".screens.FeedUserProfile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".screens.Upload" />
        <activity android:name=".screens.Register" />
        <activity
            android:name=".screens.LandingPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".screens.Login" />
        <activity
            android:name=".screens.NavigationActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>